export function SiteFooter() {
  return (
    <footer className="mt-20 border-t border-[var(--border-soft)] py-9 text-[13px] text-[var(--dim)]">
      <div className="wrap flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
        <span>AI Creator Ops — Build the creator business, not just the character.</span>
        <span>Canonical domain: aicreatorops.com</span>
      </div>
    </footer>
  );
}
